/**
 * TOMBSTONE — retired 21.9.2026. The five-door wall became Quick Pick
 * (`app/trivia/QuickPick.tsx`): one tap into הכול מהכול, seven topics, era chips from
 * the data, Revenge. Left inert so a tree that still carries this path compiles; it
 * imports nothing (rule 26).
 */
export function TopicWall(): null {
  return null
}
