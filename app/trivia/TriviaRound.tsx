/**
 * TOMBSTONE — retired 21.9.2026. The one-question-per-route round had been dead code
 * since the run moved onto one screen (`TriviaRun.tsx`); its grader signature went with
 * the Question Master, which grades by question id. Inert, imports nothing (rule 26).
 */
export function TriviaRound(): null {
  return null
}
