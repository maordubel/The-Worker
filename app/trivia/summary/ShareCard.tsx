/**
 * TOMBSTONE — retired 1.9.2026, replaced by `components/share/ShareRow.tsx`.
 *
 * The old card drew itself through an SVG `foreignObject`, which cannot embed webfonts
 * reliably; the replacement draws on a canvas at true story size. Left inert so a tree
 * that still carries this file compiles. Nothing imports it.
 */
export {}
