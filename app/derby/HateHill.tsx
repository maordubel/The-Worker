/**
 * TOMBSTONE — retired 21.9.2026. The king-of-the-hill run became the Black Wall
 * (`app/derby/HateWall.tsx`, v3): eight rounds, wall damage, one revenge, no-mercy
 * rounds and a Wall DNA code — and none of the "king" vocabulary the brief bans (§21).
 * Inert, imports nothing (rule 26).
 */
export function HateHill(): null {
  return null
}
