/**
 * TOMBSTONE — retired 1.9.2026, moved to `app/derby/file/BlackFile.tsx`.
 *
 * Gate 11's front room is now משחק השנאה and the black file is its second act, so the
 * component moved down a folder. Left inert so a tree that still carries the old path
 * compiles. Nothing imports it.
 */
export {}
