/**
 * TOMBSTONE — retired 1.9.2026. Gate 11 is now `HateHill.tsx`.
 *
 * This file shipped for one day as a knockout bracket of eight. Maor asked for the
 * opposite shape — always head to head, whoever you pick stays on, ten duels a run —
 * so the component was replaced and `hate-run.ts` lost `nextRound` and `BRACKET_SIZE`.
 *
 * It stays here inert because a delta reaches the repo through GitHub's web upload,
 * which never deletes: a tree still holding the old file would import members that no
 * longer exist, and that is a red deploy. A retired file always compiles.
 *
 * Exports nothing, imports nothing.
 */
export {}
