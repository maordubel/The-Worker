/**
 * TOMBSTONE — retired 1.9.2026. Gate 8 is now `GoalRun.tsx`.
 *
 * This file asked for a PIXEL on a cream diagram and finished with a "שלח לאימות"
 * button. Both were wrong: a match report describes a move in words, so a pixel target
 * invents a precision no source has, and a submit button is the "next" button rule 21
 * exists to forbid. The rebuild plays on twenty zones, whistles itself, and runs three
 * goals to a session.
 *
 * It stays here inert because a delta reaches the repo through GitHub's web upload,
 * which never deletes: a tree still holding this file would import `submitPath`, which
 * no longer exists, and that is a red deploy.
 *
 * Exports nothing, imports nothing.
 */
export {}
