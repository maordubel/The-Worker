/**
 * TOMBSTONE (rule 26) — retired 21.9.2026.
 *
 * The two-corner archive wing (היום לפני · הידעת) became the Living Archive of gate 12
 * v10: `components/archive/ArchiveApp.tsx`, reading the Entity Graph through
 * `lib/archive/wing.ts` and `lib/archive/graph.ts`. This file stays so a delta that
 * cannot delete (GitHub web upload) never leaves a broken import behind. It imports
 * nothing and renders nothing.
 */
export function ArchiveWing(): null {
  return null
}
