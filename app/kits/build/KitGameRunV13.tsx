/**
 * TOMBSTONE — שער 4 V13 (העלאה של 21.9.2026) הוחלף.
 *
 * V13 ניגן חולצה אחת קבועה (2009/10) ושלח את התשובה לדפדפן. המשחק הקנוני של שער 4 הוא
 * `app/kits/build/KitGameRun.tsx` על מנוע החולצות האחד (`lib/kit/engine.ts`), עם סבבים
 * מהארכיון והערכה בשרת. הקובץ נשאר כדי שהעלאה דרך הדפדפן — שמוסיפה ולא מוחקת — לא
 * תשאיר ייבוא שבור (כלל 26).
 */
export {}
