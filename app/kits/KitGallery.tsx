/**
 * TOMBSTONE · מצבה — retired 2.9.2026 when gate 5 became אגף המדים.
 *
 * This drew a "rack" of `PlayerFigure`s wearing the verified maker/sponsor of each
 * season. It is replaced by `KitWing.tsx`, which shows all 33 archive kits as a
 * collection you fill by assembling them in gate 4 — the same data, with a reason to
 * come back to it.
 *
 * `verifiedKitSeasons()` in `lib/game/kits.ts` is NOT retired: it still backs the
 * sponsor/maker verification tests. Only this screen is gone.
 *
 * Kept as an inert file rather than deleted (rule 26): deltas reach the repo through
 * GitHub's web upload, which adds and overwrites but never deletes.
 */
export {}
