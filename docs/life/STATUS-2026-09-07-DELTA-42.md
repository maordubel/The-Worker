# מצב · דלתא 42 · 7.9.2026 — מונטיזציה, רקעים, וסגירת הרשימה

## 1. מונטיזציה — שלב 1 מלא, כבוי כברירת מחדל

`lib/life/monetization/` — שכבה שהיא **שירות סביב המשחק**, לא משהו שהמשחק בנוי סביבו.

| קובץ | מה הוא עושה |
|---|---|
| `types.ts` | נקודות בטוחות, סוגי מודעה, ממשק ספק, מבנה המדיניות |
| `policy.ts` | **כל המספרים במקום אחד**, שמרניים: 240 דקות משחק בין מודעות · 5 דקות ריצה לפני הראשונה · מקסימום 3 בסשן · שקוף לגמרי במצב תומך |
| `providers.ts` | ספק ריק + עטיפת Google H5 Ad Placement API עם timeout של 4 שניות |
| `director.ts` | היחיד שהמשחק מדבר איתו. מחליט, מודד, ונועל |
| `rewards.ts` | "ארכיון נוסף" — שלושה פריטי בונוס |

**מה שהמערכת מבטיחה, ויש על זה 15 טסטים:**

- **אף פעם בתוך אירוע־אב.** נעילה מרגע פתיחת הפרק ועד שהסוף שלו נכתב ליומן — ב-2.5.1998
  זה כולל את החגיגה המוקדמת, הטרנזיסטור, פיזנט והיציאה מהשער.
- **אף פעם מוקדם, אף פעם רצוף, אף פעם הרבה.** חמש דקות ראשונות נקיות, שלוש בסשן, ובין
  שתיים — משחק אמיתי.
- **כישלון פתוח.** ספק חסר, חוסם פרסומות, רשת שנפלה, חריגה בתוך הקוד של גוגל — כולם
  מסתיימים באותה תשובה, והמשחק ממשיך. יש טסט שמפיל ספק בכוונה ומוודא שכלום לא נשבר.
- **פרס אף פעם לא שער.** כל דגל פרס מתחיל ב-`archive:`, ויש טסט שסורק את **כל** הביטים
  במשחק ונופל אם אחד מהם מותנה בדגל כזה. בנוסח שלך: אי אפשר לקנות התקדמות.
- **מצב תומך מוכן** (`isAdFree`) — פרס ניתן בלי מודעה בכלל.

**כבוי:** `NEXT_PUBLIC_ADS_ENABLED` לא מוגדר → הספק הריק → שום שינוי בהתנהגות. זה מכוון:
עד שחשבון H5 Games מאושר, בילד שמדפיס חשיפות זה בילד שמייצר משהו שאף אחד לא אישר.
מזהה המפרסם נלקח מ-`lib/ads.ts` (`ca-pub-2094079368271424`) — לא הומצא ולא הוכפל.

**בפאנל הדיבאג** יש עכשיו בלוק MONETIZATION: ספק, מצב, נעילה, כמה הוצגו, כמה שוחק מאז,
ומה הייתה התוצאה האחרונה.

**מה נשאר להפעלה בגוגל (לא משהו שאני יכול לעשות):** אישור H5 Games Ads לחשבון, ואז
להדליק את `NEXT_PUBLIC_ADS_ENABLED`.

## 2. הפנורמות שלך — נכנסו

ארבע התמונות (טיילת ×2, אלנבי ×2) הן 2.63:1 — רחוב שהולכים לאורכו, לא חדר. כל אחת נכנסה
פעמיים: **חתוכה לצורת חדר** (1600×900, המידה של כל רקע במשחק) ו**רחבה** (`-wide`), כי
מסע הטיילת דרום→צפון יזוז לאורכה כשתשלח את הגיאוגרפיה.

**על חוק הצהוב:** שאלתי אם להחריג שקיעה — התשובה מהמדידה היא שאין צורך. הכתום של השקיעה
יושב מתחת ל-hue 38, כלומר **מחוץ** לפס האסור; רק 0.71% מהתמונה היה בפנים. הטיפול לא נגע
בשקיעה עצמה. כל ארבע: **0% צהוב**, וכולן קוונטו ל-256 צבעים כמו כל רקע אחר במשחק (830–960
KB במקום 2.4MB).

מפתחות: `promenade`, `promenadeDusk`, `allenbyShops`, `allenbyShopsLate` — עם רצועות
שמיים/קרקע, בדיוק כמו כל רקע.

## 3. אימות

**1,100 טסטים · 48 קבצים · עוברים.** `tsc` נקי · `repo:hygiene` נקי · `next build` עובר ·
אודיט מבוי-סתום 0 חורים · אודיט גרפיקה: 595/603 (8 קלוז־אפים, כמו קודם).

חדשים בסבב: 15 טסטים למונטיזציה. מצטבר בשלושת הסבבים האחרונים: 12 לזרימה, 15 למונטיזציה,
+ הקנון של 1998.

## 4. מה עוד פתוח

**ממתין לך:**
1. **ניקוי שורש הריפו** — 128 קבצים, ההוראות בקליקים ב-`docs/REPO-CLEANUP-2026-09-07.md`.
   כל עוד זה לא נעשה, ה-CI ייפול (וזה בכוונה).
2. **8 קלוז־אפים** — `docs/life/GRAPHICS-NEEDED-2026-09-07.md`.
3. **גיאוגרפיית הטיילת** (משימה 30) — עכשיו יש רקע, חסרה רק הרשימה: מה עוברים, בסדר הזה.
4. **מרווח פתחים** (28) · **נגנים אמיתיים** (31) · **קול קריעת נייר**.
5. **אישור H5 Games בגוגל** — ואז מדליקים.

**מהמסמכים הטכניים שלך, לסבבים הבאים** (לא נגעתי — הם פרויקט בפני עצמו):
`ParallelHistoricalDirector` משותף ל-1990/1998 · נתוני מִשְׁחָק קנוניים עם provenance ·
נקודות שמירה לאירועי־אב · שדרוג 1999 ו-2000 · פיצול `LifeStage.tsx`.

## ידני

1. **קודם** ניקוי השורש (`REPO-CLEANUP`).
2. שני ZIP → GitHub → Add file → Upload files → **לגרור תיקיות, לא קבצים** → Commit.
3. לשונית Actions — לראות שה-CI ירוק.
