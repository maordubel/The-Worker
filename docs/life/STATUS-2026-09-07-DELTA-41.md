# מצב · דלתא 41 · 7.9.2026 — בטיחות ריפו, זרימה, וקנון

צדקת. אני מתקן דבר אחד ושובר אחר כי אין שער שבודק. **בדלתא הזאת בניתי את השער.**

## 1. הריפו — מקור אחד לכל דבר

בדקתי את `origin/main` עצמו, לא את העותק שלי. בשורש יושבים **128 קבצים** שלא אמורים
להיות שם: 51 קבצי קוד (`LifeStage.tsx`, `WorldScene.ts`, `dialogue (1).ts`, 11 קבצי
פרקים), 55 נכסים, 22 מסמכי היסטוריה. הם הגיעו מהמעלה של GitHub — גרירת **קבצים** במקום
**תיקיות** משטחת הכול לשורש.

**ההוכחה שמותר למחוק** (השוואה מלאה, לא ניחוש):
- אף אחד מהם לא מיובא. `tsconfig.json` כולל רק `app/`, `components/`, `lib/`, `types/`.
- 16 מהם **זהים בדיוק** לקנוני.
- 34 **קטנים** מהקנוני — גרסה ישנה. `WorldScene.ts` בשורש 155KB מול 202KB;
  `chapterStageA.ts` 41KB מול 75KB.
- `dialogue (1).ts` — עותק ישן של `lib/life/runtime/dialogue.ts`, לפי שורות הייבוא.

**אין ולו שורה אחת בשורש שלא קיימת, חדשה יותר, במקום הנכון.** הרשימה המלאה והוראות
המחיקה בקליקים: `docs/REPO-CLEANUP-2026-09-07.md` (מצורף).

מה שנבנה כדי שזה לא יחזור:
- **`npm run repo:hygiene`** — נופל על קוד/נכסים בשורש, על שמות־עותק, ועל כל קובץ ששמו
  כבר קיים תחת תיקיית ייצור.
- **CI ב-GitHub** (`.github/workflows/ci.yml`) — כל דחיפה: hygiene → typecheck → tests →
  build. בלי זה "תוקן" זה משפט, לא עובדה.
- **`CLAUDE.md` חוק 59** — אסור לערוך עותק שורש, אסור למסור קבצים לשורש, וכל תיקון מדווח
  את הנתיב הקנוני.
- **ארכיון** — 17 מסמכי ההיסטוריה שהיו רק בשורש נשמרו ב-`docs/implementation-history/`.

## 2. הזרימה — "תסתכל על הרצפה" כבר לא חוסם פרק

הבאג שדיווחת עליו, בקוד: הערב באוסישקין נסגר על `saw:parquet` **וגם** `saw:stand` — שתי
נקודות לחיצה קטנות על רצפה ועל מעקה. ילד שנכנס עם אפי, שאפי הראה לו את המקום ושעמד שם
חצי שעה — חיכה לרשת הביטחון של 20:40.

**מה שנבנה:**
- **`lib/life/world/milestones.ts`** — אבן דרך סמנטית: `life:seen:ussishkin` מתקיימת גם
  משתי הנקודות **וגם** מעצם זה שאפי הראה לו את המקום. הפרק מתנה על החוויה, לא על הלחיצה.
- **פיוס מצב (`reconcile`)** — רץ בכל דקה ובכניסה לחדר: אם העולם מוכיח שמשהו קרה, הדגל
  מתעדכן. הוא מעלה דגלים בלבד — לא חפץ, לא קשר, לא זיכרון (מה שהמסמך שלך דורש בסעיף 7).
- **סדר הפעולות** — אם דקה תיקנה משהו, הביטים של הפרק מקבלים את ההזדמנות שלהם **לפני**
  רשת הביטחון. הרובוט מצא בדיוק את זה: "התנאי מתקיים — הביט פשוט לא רץ", כי הרשת הקדימה
  אותו. יום שנסגר במשפט שמישהו כתב תמיד עדיף על יום שנגמר כי הזמן אזל.
- **`lib/life/world/flow.ts` + כרטיס «יש לך עוד קצת זמן»** — כשהדבר הבא שהיום רוצה חסום
  **רק על השעון**, המשחק אומר את זה ומציע לקפוץ. הכרטיס אף פעם לא כופה: יש עליו
  «להסתובב», והקפיצה נוחתת שלוש דקות לפני הביט כדי שהביט עדיין ישוחק במקום שנכתב לו.
- **אבחון (`__life.debug.flow()`)** — בפיתוח בלבד: כמה דקות שקט, מה חוסם, ובאיזו דקה.

**5 טסטים חדשים ששומרים על זה לתמיד** (`tests/life-flow.test.ts`): אף פרק לא נסגר על
דגל־תפאורה (`saw:*`); לכל פרק יש דרך כתובה להיגמר; אבן הדרך מתאוששת משתי הדרכים ולא
מומצאת למי שלא נכנס; שער־זמן יודע לנקוב בדקה; ואף חדר בעולם אינו ללא יציאה.

## 3. הקנון — 2.5.1998 הוא המחזור ה-29

המשחק קרא לו "המחזור האחרון" בחמישה מקומות: כותרת העוגן, שורת העזרה, כרטיס הפרק,
המטרה ופתיח הביט. הליגה נגמרה ב-9.5.1998. תוקן בכולם, ויש טסט שסורק את חמשת הקבצים
ונופל אם המשפט חוזר.

## 4. גרפיקה — מה שאני צריך ממך

`docs/life/GRAPHICS-NEEDED-2026-09-07.md` (נשלח קודם), ומעכשיו הוא **נוצר מהקוד**:
`npx tsx scripts/life/art-audit.ts --md`. 591 מתוך 599 הנכסים קיימים; חסרים 8 קלוז־אפים
(כרגע נופלים חזרה לפורטרט, שום דבר לא שבור).

## 5. מה שעדיין לא סגור — בכנות

- **הרובוט עדיין סוגר את a2-alley ו-a3-hall דרך רשת הביטחון.** הסיבה שונה ממה שחשבתי:
  ברוב הריצות הוא פשוט לא מבצע את הפעולה שהיום מבקש (להיכנס לסמטה / להיכנס לאולם),
  והמטרה נשארת פעילה — כלומר זה לא "יום שאין בו מה לעשות" אלא רובוט שלא מצא את מה שיש.
  החסימה שדיווחת עליה — הפרקט — תוקנה ומכוסה בטסט. את שאר הדרך אני צריך לראות בעיניים
  שלך: אם אתה נתקע במקום מסוים, תגיד לי איפה ובאיזו שעה ואני אלך ישר לשם.
- **הפנורמות החדשות** (טיילת ×2, אלנבי ×2) — התקבלו. הן 2.63:1, כלומר רקעים רחבים ולא
  פנורמות 360°, ומתאימות בדיוק למסע הטיילת (משימה 30) ולאלנבי. שאלה אחת לפני שאני
  מטמיע: **השקיעה.** חוק 8 מוריד כל צהוב לחום — על שקיעה מעל הים זה יהרוס את התמונה.
  להחריג רקעי שקיעה מהחוק, או להטמיע עם הטיפול?
- **תוכנית המונטיזציה** — התקבלה, נכנסת כמשימה. לא נגעתי בה בסבב הזה כדי לא לפתוח חזית
  נוספת לפני שהשער ירוק.

## אימות הסבב

1,085 טסטים (47 קבצים) · `tsc` נקי · `repo:hygiene` נקי · `next build` עובר · אודיט
מבוי-סתום 0 חורים.

## ידני

1. **קודם** — ניקוי השורש לפי `docs/REPO-CLEANUP-2026-09-07.md` (github.dev, שתי דקות).
2. אחר כך להעלות את ה-ZIP: **לפרוס, ולגרור את התיקיות** — `app`, `components`, `lib`,
   `scripts`, `tests`, `docs`, `.github` — **ולא את הקבצים שבתוכן**. זה בדיוק ההבדל בין
   קובץ שנוחת ב-`lib/life/` לקובץ שנוחת בשורש.
3. אחרי הדחיפה — לפתוח את לשונית **Actions** בגיטהאב ולראות את ה-CI רץ.
