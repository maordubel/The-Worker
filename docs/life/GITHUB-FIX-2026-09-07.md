# מה חסר ב-GitHub כרגע — ושלושה קבצים שהעלאה רגילה לא יכולה להעביר

> נבדק מול `github.com/maordubel/The-Worker` ב-7.9.2026, אחרי שהעלית את דלתא 43.

## מה תקין

דלתא 43 נחתה במלואה — 82 קבצים. השוויתי בייט־בבייט את `app`, `components`, `lib`, `scripts`, `tests`,
`content/manual` ו-`messages` מול מה שיש אצלי: **555 קבצים, כולם זהים** חוץ מאחד (למטה).

## שלוש בעיות

### 1 · שורה אחת חסרה ב-`matchDirector.ts` — והיא שוברת את המיקס של 2.5.1998

```
if (step.listen !== undefined) this.host.emit('sound', { kind: 'listen', weight: step.listen })
```

ארבעה צעדים בתסריט "השרוכים" מגדירים `listen`, וזה כל החצי השני של המשימה: היציע חוגג בתשעים אחוז בזמן
שטרנזיסטור בחמישה אחוז מכריע את העונה. בלי השורה הזאת השדה נכתב לתוך כלום — בלי קריסה, בלי אזהרה,
בלי שגיאת טיפוסים. פשוט לא קורה.

**למה זה קרה:** `pack-delta.sh` בנה את רשימת הקבצים מ-`git status`, שעונה על "מה שונה מהקומיט האחרון שלי"
ולא על "מה שונה מ-GitHub". קובץ שנשמר אצלי בקומיט ומעולם לא הועלה נראה נקי ונשמט מכל דלתא אחריו. **תוקן** —
הסקריפט משווה עכשיו מול `origin/main` בפועל.

**ובדיקה שתופסת את זה מעכשיו:** `tests/life-match.test.ts` סורק את כל שדות הצעדים שהתסריטים מגדירים
ומוודא שהבמאי קורא כל אחד מהם. הרצתי אותו מול הגרסה שב-GitHub — הוא נופל, ואומר בדיוק
`matchDirector never reads step.listen`.

### 2 · 16 קבצי הפנורמות מדלתא 42 מעולם לא הועלו

`lib/life/runtime/art.ts` ב-GitHub כבר רושם `promenade`, `promenadeDusk`, `allenbyShops`,
`allenbyShopsLate` — אבל התמונות עצמן לא שם. הטבלה מצביעה על קבצים שלא קיימים. ה-ZIP של האמנות מצורף שוב.

### 3 · שלושה קבצים ש-GitHub פשוט משמיט בהעלאה

בדקתי: **אין אף קובץ שמתחיל בנקודה בכל הריפו.** ההעלאה הרגילה של GitHub מסננת אותם בשקט.

- `.github/workflows/ci.yml` — לכן ה-CI לא רץ בכלל. הוא בנוי, הוא פשוט לא שם.
- `.eslintrc.json` — הגדרות ה-linter.
- `.gitignore` — בלעדיו GitHub לא יודע להתעלם מ-`node_modules` ומ-`.next`.

## מה עליך לעשות — קליקים בלבד

**א · שני ה-ZIP המצורפים** — העלאה רגילה כרגיל (Add file → Upload files → גרירה → Commit).

**ב · שלושת קבצי הנקודה — דרך "Create new file", לא דרך העלאה:**

1. בריפו: **Add file** → **Create new file**.
2. בשורת השם הקלד את הנתיב **בדיוק** כך (הסלאש יוצר את התיקיות לבד):
   `.github/workflows/ci.yml`
3. פתח את `.github/workflows/ci.yml` מתוך ה-ZIP, העתק את התוכן, הדבק בחלון.
4. **Commit changes**.
5. חזור על 1–4 עבור `.eslintrc.json` ועבור `.gitignore` (אלה בשורש, בלי סלאש).

**ג · מחיקת קבצי השורש** — עדיין לא בוצעה. 12 קבצים נשארים, כל השאר מת. הפירוט המלא ב-`REPO-CLEANUP-2026-09-07.md`.
