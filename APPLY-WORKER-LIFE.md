# THE WORKER LIFE — עדכון אמנות (Stage A)

דלתא בלבד: רק מה שנוסף או השתנה, במבנה התיקיות האמיתי.

## מה להעלות

1. לפתוח `github.com/maordubel/The-Worker` בדפדפן.
2. **Add file → Upload files**.
3. לגרור פנימה את התיקיות מהזיפ — `app`, `components`, `lib`, `public`, `scripts`,
   `docs`, `messages`, `tests` — ואת הקבצים הבודדים `CLAUDE.md`, `package.json`,
   `package-lock.json`. גרירת תיקייה שומרת על המבנה.
4. **Commit changes**. ורסל בונה לבד.

הכתובת: **/life**, וגם לוחית כניסה גדולה בראש הקיר בעמוד הבית.

> אם זו העלאה שנייה (כבר העלית את המשלוח הקודם): הקבצים פשוט נדרסים. אין מה למחוק.

## מה יש בפנים

- `public/life/art/` — **48 קבצי אמנות** שנחתכו מלוחות הקונספט שלך. כל רקע וכל דמות
  במשחק היא מלבן מתוך אחד הלוחות.
- `scripts/life/art-manifest.json` — הרשימה: איזה מלבן מאיזה לוח הפך לאיזה נכס.
- `scripts/life/build-art.py` — הסקריפט שחתך אותם. **לא צריך להריץ אותו**; הפלט כבר בזיפ.
  הוא שם כדי שחיתוך מחדש יהיה שינוי מספר, לא ציד.
- `lib/life/world/scenes.ts` — העולם החדש: תמונה אחת לכל מקום + רצועת הליכה.

## אם תרצה לשנות חיתוך של נכס

לפתוח את `scripts/life/art-manifest.json`, לשנות את המלבן, ולומר לי — אני מריץ ומחזיר
PNG חדש. שום דבר במשחק לא זז: כל מיקום בסצנה הוא אחוז מהתמונה, לא פיקסל.

## עדיין פתוח (לא באג — לא נבנה בכוונה)

- דמות אחת לכל אדם. הלוחות הם אמנות מצגת, לא sheet של אנימציה, אז ההליכה היא נדנוד
  ונטייה ולא מחזור הליכה.
- קובי ורחל מצוירים בלוח מהמותניים ומעלה, אז הם מוצבים איפה שזה נכון: יושב על הספה,
  מאחורי שולחן המטבח, בתוך יציע צפוף.
- אוסישקין — שני הרקעים חתוכים ובזיפ, אבל עוד לא בסצנה. שמור ומוכן, בלי גיימפליי.
- **אין סאונד בכלל.** זה הדבר הבא עם הכי הרבה החזר.
