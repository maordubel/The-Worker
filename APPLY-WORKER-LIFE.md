# THE WORKER LIFE — מה לעשות עם הקובץ הזה

זו דלתא בלבד: רק הקבצים שנוספו או השתנו, במבנה התיקיות האמיתי של הפרויקט.

## מה להעלות

1. לפתוח `github.com/maordubel/The-Worker` בדפדפן.
2. **Add file → Upload files**.
3. לגרור לתוך החלון את **התיקיות** מתוך הזיפ (`app`, `components`, `lib`, `docs`,
   `messages`, `scripts`, `tests`) ואת הקבצים הבודדים (`CLAUDE.md`, `package.json`,
   `package-lock.json`). גרירה של תיקייה שומרת על המבנה — אין צורך להיכנס פנימה.
4. **Commit changes**. ורסל תבנה לבד.

הכתובת החדשה אחרי הבנייה: **/life**, ויש גם כניסה גדולה בראש הקיר בעמוד הבית.

## דבר אחד שכדאי למחוק (לא חובה, לא קשור למשחק)

בריפו יושבים שלושה קבצים ישנים ששרדו העלאות קודמות ושוברים את `npm run test`
ואת `npm run typecheck` — הם לא בשימוש ולא משפיעים על הבנייה בוורסל:

```
lib/ingest/dedupe.ts
lib/ingest/guards.ts
lib/ingest/types.ts
```

למחוק בקליקים: לפתוח כל קובץ ב-GitHub → כפתור פח האשפה למעלה מימין → **Commit changes**.
(זה בדיוק המקרה שחוק 26 מדבר עליו: העלאה דרך הדפדפן מוסיפה ודורסת, אבל אף פעם לא מוחקת.)

## מה חדש בקבצים

- `package.json` — נוספה תלות אחת: **phaser**. ורסל מתקינה לבד בבנייה.
- `messages/he.json` — 16 מפתחות חדשים תחת `life.*`.
- `app/page.tsx` — לוחית כניסה מעל קיר השערים.
- `lib/game/archive.ts` — שני שדות אופציונליים (`sport`, `clubSlug`) נחשפו בטיפוס
  הגביעים, כדי שהמשחק יוכל לסנן לפי ענף כפי שחוק 6 דורש.
