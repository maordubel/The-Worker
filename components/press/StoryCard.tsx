/**
 * TOMBSTONE — retired 1.9.2026, replaced by `lib/share/story.ts`.
 *
 * There is exactly one share system and it is the canvas renderer. Left inert so a tree
 * that still carries this file compiles. Nothing imports it.
 */
export {}
