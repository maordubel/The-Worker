/**
 * TOMBSTONE · מצבה — retired 15.9.2026, replaced by `components/life/TunnelPlate.tsx`.
 *
 * THE WORKER LIFE stopped hanging above the gate wall as its own plate and moved INTO
 * the wall as a full-width tunnel plate, first in the grid, above gate 5 — same plate
 * anatomy as a gate, labelled a tunnel so it can never be read as gate 9 or gate 12
 * (rule 24, rule 39).
 *
 * Kept as an inert file rather than deleted: deltas reach the repo through GitHub's web
 * upload, which adds and overwrites but never deletes, so a removed file with live
 * imports keeps failing the deploy long after the local tree is clean (rule 26). This
 * imports nothing and exports nothing anyone renders.
 */
export {}
