-- =====================================================================
-- 20260920090000_royal_rumble_live.sql — ריק בכוונה (22.9.2026). אל תריצו.
-- =====================================================================
--
-- רויאל ראמבל חי (20.9). הקובץ לא הורץ אף פעם, והתוכן שלו עבר, בשמות worker_ ובלי שום נגיעה ב-auth,
-- לקובץ אחד: supabase/migrations/20260922090000_worker_shared_project.sql
--
-- הסיבה: הפרויקט ב-Supabase משותף עם DUBID. הגרסה הישנה שמה טריגר על כל הרשמה בפרויקט
-- ויצרה כרטיס לכל משתמש קיים — כלומר נגעה במשתמשים של DUBID. ההיסטוריה המלאה בגיט.
-- הקובץ נשאר כאן ריק ולא נמחק, כי העלאה דרך GitHub לא מוחקת קבצים (כלל 26).
select 'הקובץ הזה ריק. הקובץ היחיד שמריצים הוא 20260922090000_worker_shared_project.sql' as note;
