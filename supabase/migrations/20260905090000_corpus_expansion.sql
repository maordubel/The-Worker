-- =====================================================================
-- 20260905090000_corpus_expansion.sql — ריק בכוונה (22.9.2026). אל תריצו.
-- =====================================================================
--
-- זו הייתה סכמת הארכיון (מקורות, מועדונים, משחקים, קורפוס הוויקי). האתר לא קורא אותה בזמן
-- ריצה — השערים קוראים את content/ מהריפו — ורק שורת הפקודה של הקליטה (npm run ingest) כותבת
-- אליה. היא יוצרת ב-public טבלאות בשמות כלליים (source, club, match, person...), ולכן אסור
-- להריץ אותה בפרויקט Supabase שמשותף עם DUBID.
--
-- התוכן המלא, בלי שינוי: supabase/archive-schema/20260905090000_corpus_expansion.sql — לפרויקט ארכיון נפרד, אם ייפתח.
-- הקובץ נשאר כאן ריק ולא נמחק, כי העלאה דרך GitHub לא מוחקת קבצים (כלל 26).
select 'הקובץ הזה ריק. הקובץ היחיד שמריצים הוא 20260922090000_worker_shared_project.sql' as note;
