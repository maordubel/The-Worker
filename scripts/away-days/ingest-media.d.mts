/** Types for the plain-node photo pipeline (`ingest-media.mjs`), so a test can prove its yellow is the rule's. */
export function isYellow(r: number, g: number, b: number): boolean
export function countYellow(data: Uint8Array, channels: number): number
