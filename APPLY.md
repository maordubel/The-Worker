# The Worker — דלתא 9 (מצטברת), 1.9.2026

פרוס על שורש הריפו והעלה. **אין מה למחוק ואין פקודות להריץ.**

`npm run typecheck` נקי · `npm run lint` נקי · `npm run test` **264 עוברים** ·
`npm run build` ירוק · רספונסיב נקי · אפס פיקסלים צהובים.

**הדלתא הזאת מצטברת: 7, 8 ו-9 יחד** — 71 קבצים. אם פספסת אחת מהקודמות, זו מדביקה הכל;
אם כבר העלית אותן, תעלה את זו מעליהן והקבצים זהים.

בפנים: משחק השנאה ראש־בראש, שחזור השער, אגף אוסישקין, חיפוש השחקנים (7) · הסטורי
המשודרג, אגף הטריוויות, הפרסומות, פס הקרדיט, הגרפיקות שלך (8) · **יבואן ויקיפועל** (9).

בזמן הבנייה תפסתי שאני בונה אותה על בסיס שגוי — היא הייתה יוצאת בלי הקבצים של דלתא 8.
בניתי מחדש כמצטברת, וזו הסיבה שהיא גדולה יותר.

---

## קודם — תיקון שאני חייב לך

כתבתי לך שוב ושוב ש"ויקיפועל מחזירה 403 לקריאה אוטומטית". **הטענה הזאת לא מבוססת.**

נכון שקיבלתי 403. אבל בדקתי גם את ויקיפדיה מאותו קונטיינר:

```
he.wikipedia.org  403  Host not in allowlist: he.wikipedia.org
en.wikipedia.org  403  Host not in allowlist: en.wikipedia.org
```

הסביבה שאני רץ בה מחזיקה **רשימת אתרים מותרים**, ולא ויקיפדיה ולא ויקיפועל נמצאות בה.
כלומר **אני לא יכול לדעת מכאן** אם ויקיפועל חוסמת לקוחות API בכלל — ייתכן מאוד שלא, ושכל
מה שראיתי הוא הארגז שלי.

תיעדתי את זה כחוק (`CLAUDE.md` 11): מקור חסום מתועד עם הראיה שאומרת **מי** חסם, או מתועד
כלא ידוע. **מה שזה אומר לך: קרוב לוודאי שהיבואן ירוץ מצוין מהמחשב שלך.**

---

## היבואן

```bash
npm run wiki:corpus -- --dry-run --limit 40    # בדיקת עשן, בלי מסד
npm run wiki:corpus -- --dry-run               # כל הוויקי, לדיסק
npm run wiki:corpus                            # כל הוויקי, לסופאבייס
npm run wiki:corpus -- --dump export.xml       # מקובץ ייצוא, בלי רשת בכלל
npm run wiki:corpus -- --fresh                 # להתעלם מנקודת העצירה
```

### ארבעה דברים שיבואן תמים לא עושה

**1 · העימוד עובר את התקרה.** `aplimit=max` מחזיר 500 לאנונימי ו-5,000 לבוט, ו**שניהם
לא נקודת עצירה**. ה-API מחזיר פרוסה חתוכה ועוד סמן, והקריאה הנכונה היחידה היא להמשיך
אחרי `apcontinue` עד שאין. הגילוי הוא גנרטור, אז ויקי בכל גודל זורם בזיכרון קבוע והתוכן
נכנס בזמן שהגילוי עוד רץ.

**2 · כל מרחב שמות בהליכה נפרדת.** `apnamespace` מקבל ערך אחד בדיוק. להעביר כמה קורא רק
את הראשון — באג שנראה כמו ייבוא מלא וחסר בו רוב הוויקי.

**3 · רשימות מאפיינים ממוזגות, לא נחתכות.** זה זה שמאבד מידע בשקט: גם `prop=links` חתוך.
דף עם 700 קישורים מחזיר 500 ועוד `plcontinue`, ולקוח שקורא פעם אחת שומר דף שחסרים בו 200
קישורים — ונראה תקין לגמרי.

**4 · סירוב לא מנוסה שוב.** כשל רשת, 429 ו-5xx נסוגים ומנסים שוב ומכבדים `Retry-After`.
**403 ו-404 לא.** לנסות שוב סירוב זה איך שיבואן מנומס הופך לאיל ניגוח.

**ועוד:** נקודת עצירה אחרי כל אצווה (ריצה שנפלה בדף 4,000 מתוך 6,000 ממשיכה משם), והשוואת
hash לפני כתיבה — דף שלא השתנה מקבל נגיעה ב-`last_seen_at` וזהו.

### לאן זה נכנס

שורה אחת לכל דף ב-`raw_wiki_page` הקיימת, **לפי ה-`page_id` של הוויקי**. בחרתי בזה ולא
בכותרת כי דף ששמו משתנה שומר את המזהה — יבואן ממופתח-כותרת היה יוצר שורה שנייה בכל פעם
שעורך מזיז דף.

כל שורה נושאת את הכתובת המקורית, מזהה הדף, **הוויקיטקסט המלא בלי שינוי**, hash,
מזהה/זמן/משתמש/הערת הרוויזיה, והקטגוריות/קישורים/תמונות כפי שה-API פותר אותם.

שתי תצוגות: `v_wiki_corpus_summary` (דפים, הפניות, ריקים ובייטים לכל מרחב שמות) ו־
`v_wiki_category_sizes` (כל קטגוריה וכמה דפים בה). **השנייה היא איך בודקים שהייבוא שלם** —
השירים, העונות והמשחקים מגיעים כקטגוריות, והגדלים שלהן הם התשובה ל"קיבלנו הכל?".

### המסלול בלי רשת

אם ה-API באמת חסום: דפדפן → `Special:Export` → "רק הגרסה הנוכחית" → XML אחד, ו-`--dump`
קורא אותו. אותן רשומות, אותה טבלה, אותה אידמפוטנטיות, אפס בקשות רשת.

---

## האימות — מה באמת נבדק

לא יכולתי להגיע לוויקי, אז לא הבטחתי — **הרמתי MediaWiki מדומה שאוכף את אותם אילוצים**
והרצתי מולו את הקוד האמיתי:

- **1,200 דפים מאחורי תקרה של 500 לתשובה** — יבואן תמים היה מביא 500 ונראה מוצלח
- **דף עם 700 קישורים מאחורי סמן** — מי שלא ממזג מאבד 200 בשקט
- **שני מרחבי שמות** בהליכות נפרדות
- **429 על הבקשה הראשונה מכל סוג**, כדי שנתיב הניסיון החוזר ירוץ בכל הרצה

14 טסטים, כולם עוברים: הגילוי מלא (1,200/1,200), התוכן זהה למקור, הרשימות ממוזגות
(700/700), כותרת חסרה מדווחת ולא מומצאת, נקודת העצירה ממשיכה — **וריצה שנייה מכניסה 0
ומעדכנת 0**, ובכל זאת מזהה דף אחד שבאמת השתנה ומעדכנת אותו במקום.

### וההרצה מול הוויקי האמיתית

```
$ npm run wiki:corpus -- --dry-run --limit 40
MediaWikiAccessError: request rejected —
  https://wiki.red-fans.com/api.php?...&siprop=namespaces (HTTP 403)
```

נכשל על הבקשה הראשונה, **ולא ניסה שוב** — בדיוק כמו שתוכנן.

---

## מה שאתה צריך לעשות

1. **תריץ מהמחשב שלך:** `npm run wiki:corpus -- --dry-run --limit 40`. עובד? תוריד את
   ה-`--limit` ותן לזה לרוץ.
2. **גם אצלך 403?** `Special:Export`, ותשלח לי את ה-XML.
3. **תשלח לי את `data/reports/wiki-corpus-*.json`** — שם כתוב כמה דפים, אילו קטגוריות ומה
   נכשל. משם אני בונה את ON TOUR, את חדר ההלבשה ואת שאר המאגר.

**על השירים:** הטקסט הגולמי נשמר כי המקוריות והאידמפוטנטיות צריכות אותו, אבל שאלה נבנית
מה**מטא-דאטה** — השם, הלחן, על מי. שום שאלה, הסבר או כרטיס שיתוף לא מדפיס בתים. זה כתוב
כחוק ב-`CLAUDE.md` ובתבניות.

---

## פתוח

- **שער 7 סקרים · שער 9 חדר הלבשה · שער 13 ציר הזמן** — דלתא הבאה
- **שער 12 ON TOUR** — נפתח ברגע שהוויקי נכנסת
- **הסרטון Hot Red Splash** — ממתין להחלטה שלך (מדי היריבה בצהוב)
- **אמיר גולה / שלום תקווה** — אין מקור
