# The Worker — דלתא 15 (מצטברת), 2.9.2026

פרוס על שורש הריפו והעלה. **אין מה למחוק ואין פקודות להריץ.**

`npm run typecheck` נקי · `npm run lint` נקי · `npm run test` **332 עוברים** ·
`npm run build` ירוק · `npm run qa:sweep` — 56 מסכים, אפס גלישה, אפס שגיאות, אפס צהוב.

**מצטברת: 7 עד 15 יחד** — 120 קבצים.

---

## שער 5 · אגף המדים — מה שהבטחתי בדלתא הקודמת

שלוש תצוגות של אותן 33 חולצות, והטאב הוא הניווט היחיד כי אלה שלושה עומקים של דבר אחד
ולא שלוש תכונות:

**האוסף** — 33 חולצות מהארכיון. חולצה שהרכבת מצוירת; חולצה שלא — מתאר מקווקו.
**כרטיס חולצה** — חולצה אחת מקרוב, שלושה חיתוכים אמיתיים של אותו ציור (גוף · סמל ·
ספונסר), מפרט מלא, **והמקור שממנו היא צוירה**. **מעצב** — כמו שהיה.

**וזה מה שמחבר בין שער 4 לשער 5.** חולצה נכנסת לאוסף כשאתה **מרכיב** אותה, בכל ניקוד.
בלי הרישום הזה, משחק המדים הוא ניקוד שמתאדה והארכיון הוא רשימה שגוללים.

**חולצה נעולה לא מציגה כלום — וזה תיקון של באג שלי.** הגרסה הראשונה ציירה מתאר ברשת
ואז הדפיסה את הספונסר מתחתיו; ולחיצה עליה פתחה כרטיס עם **החולצה המלאה, הספונסר, היצרן
והסמל** — כלומר דף התשובות המלא לחידה של אותה חולצה בשער 4, במרחק הקשה אחת. עכשיו חולצה
שלא הרכבת מראה עונה, מתאר, ואת הדרך פנימה. יש בדיקה שנופלת אם זה יחזור.

*(גם הצפיפות תוקנה: 33 חולצות בשתי עמודות היו עמוד של 9,300 פיקסלים. שלוש עמודות בטלפון,
שש בדסקטופ.)*

## הבריף שלך על הוויקי — מה הוא שינה כבר עכשיו

קראתי אותו. שני שדות שהוא דורש והיבואן שלי **לא** סיפק:

**1 · `redirect_target` היה קבוע `null`.** באג אמיתי, לא חוסר. הפתרון המתבקש הוא עוד
קריאת API לכל עמוד — והתשובה כבר בידיים: תוכן של עמוד הפניה הוא כולו
`#REDIRECT [[יעד]]`, והיבואן שומר את הוויקיטקסט המלא של כל עמוד. אז זה **פענוח, לא
בקשה**. תומך גם ב-`#הפניה`.

**2 · `backlinks[]` לא נאספו.** `list=backlinks` עונה עמוד אחד לכל בקשה — אלפי בקשות
נוספות למידע שכבר יש. הקישורים היוצאים של כל עמוד נשמרים, אז **היפוך המפה פעם אחת** אחרי
הסריקה נותן את זה בחינם ובדיוק מלא.

**ומה שההיפוך לא יכול לדעת נאמר במפורש:** קישורים נכנסים מעמודים שלא יובאו. סריקה חלקית
נותנת backlinks חלקיים. זה רשום בקוד ובחוק 34, לא מטואטא.

*(שאר הבריף — סדר העדיפויות 1980–2000, קטגוריות הסגל, עמודי המשחקים, מילון בלומפילד,
המסמכים הסרוקים — נכנס לתיק הפרויקט. היבואן כבר סורק namespaces, קטגוריות, קישורים
ותמונות; מה שחסר לו הוא הרצה אמיתית מהמחשב שלך.)*

---

## הספר הפתוח

| מה | מצב | חסום על |
|---|---|---|
| שער 9 · חדר הלבשה | **לא נבנה — הבא בתור** | — |
| שער 12 · ON TOUR | לא נבנה | **רשימת שירים ממך** |
| THE WORKER LIFE | תכנית מאושרת? | **אישור + שנת לידה + שאלת החולצה** |
| ספירה חיה לסקרים | השכבה מוכנה | טבלה ב-Supabase |
| יבואן ויקיפועל — הרצה | הקוד מוכן ובדוק | **הרצה מהמחשב שלך** |

שער 9 הוא היחיד שנשאר עליי לבד, והוא הבא.
