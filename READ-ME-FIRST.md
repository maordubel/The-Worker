# עדכון — חסויות לפי תחרות + אתגר החולצה

## מה לעשות

1. פרוס את הקבצים מעל הריפו הקיים (אותם נתיבים בדיוק).
2. **קובץ חדש אחד** — `lib/game/seasons.ts`. ודא שהוא נכנס ל-git:
   `git add lib/game/seasons.ts`
3. אין שום קובץ שצריך למחוק מה-git הפעם.
4. הרץ מיגרציה חדשה ב-Supabase:
   `supabase/migrations/20260903090000_sponsor_by_competition.sql`
5. `npm run test && npm run build` — 164 טסטים, בילד ירוק.

## מה תוקן ומה נוסף

**התשובות שלך נכנסו לסכימה, לא להערה.**
`sponsor_deal.competition_id` — חסות יכולה להיות מוגבלת לתחרות אחת.
`null` = כל התחרויות באותה עונה.

- 2010/11 — כתר בליגת האלופות בלבד, **בוני התיכון** בליגת העל. שתי שורות נפרדות.
- 2009/10 — עונת הדאבל: אמברו + **סובארו**, בוודאות 3.
- חמש סתירות נסגרו ורשומות עם `resolvedBy: מאור הראל`. שש הסתירות האחרות
  נשארות פתוחות כי אין להן מקור מכריע.

**באג אמיתי שנמצא תוך כדי — ההצלבה בין יצרן לחסות.**
ספל של יצרן הוא טווח ("אמברו, 2006/07 עד 2010/11"), אבל הקוד השווה
`fromLabel` ל-`fromLabel`. התוצאה: כל עונה שנמצאת *באמצע* טווח נעלמה —
כולל 2010/11, בדיוק העונה שאימתת. `lib/game/seasons.ts` החדש עושה חיתוך
טווחים אמיתי.

- מסך המדים: מ-16 כרטיסים (רק תחילות ספל) ל-**45 עונות**.
- אתגר החולצה: מ-2 שאלות ל-**12**.

**אתגר החולצה (`/kits/build`) — משחק חדש, מצב 07.**
שואל על זוג `(עונה, תחרות)` שבו גם היצרן וגם החסות מאומתים.
הבדיקה בשרת; התשובה לא נמצאת ב-payload.

עונה שבה **שתי חסויות נכונות** לא נשאלת בכלל. 2019/20 היא המקרה האמיתי:
ארקיע הסתיימה מוקדם והכשרה נכנסה באמצע העונה — שתיהן נכונות, ולכן אין
לשאלה תשובה אחת. הארכיון שומר את שתיהן; המשחק לא שואל.

**באג RTL שתוקן בשלושה מקומות.**
`<Num>` הוא `<bdi dir="ltr">`. מרג'ין לוגי (`me-2`) על אלמנט כזה נפתר מול
הכיוון של האלמנט עצמו — כלומר LTR — ונחת בצד הלא נכון. לכן נראה
"2023/24כל התחרויות" בלי רווח. הרווח עבר לשורת ה-flex. יש עכשיו טסט
שנכשל אם זה יחזור.

## בדיקות

164 טסטים ירוקים · טייפצ'ק נקי · ESLint נקי · בילד ירוק
סריקה ב-320/390/430/768/1440 על שמונה מסכים — 40/40 בלי גלישה אופקית,
התפריט `fixed` ונעול לתחתית בכולם, שום דבר לא מוסתר מאחוריו.
אינג'סט: 295 נמצאו / 295 יובאו / 0 נדחו / 0 לא נפתרו.
