name: Sync LIFE patches

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply-life:
    runs-on: ubuntu-latest
    steps:
      - name: Already applied
        run: echo "LIFE patches were already applied on this sync branch."
