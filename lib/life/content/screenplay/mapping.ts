import type { ReputationAudience } from '../../types'

/**
 * מה המילים של התסריט אומרות במנוע — וכל הכרעה כתובה, לא מנוחשת (20.9.2026).
 *
 * תסריט ההמשך מדבר בשפה משלו: `מיומנויות: {"documentation": 2}`, `אמון קהילה:
 * {"terrace": 3}`, `נוכחות: venue`. למנוע יש חמישה כישורים, חמישה קהלים ושישה מצבי
 * נוכחות, והשמות אינם זהים. **הפער הזה הוא הכרעה ולא באג**, ולכן הוא יושב בטבלאות
 * מפורשות ולא בפונקציה שמנחשת: טבלה אפשר לקרוא, לחלוק עליה ולתקן; `includes('doc')`
 * אי-אפשר.
 *
 * הכלל שכל הקובץ הזה עומד עליו: **מה שאין לו בית מדווח בשמו ואינו מתורגם בערך.**
 * `npm run life:screenplay-map` מריץ את המיפוי על כל 345 הבחירות ומדפיס בדיוק מה נשאר
 * בחוץ, ו-`tests/life-screenplay.test.ts` נופל כשמפתח חדש מופיע בתסריט בלי הכרעה —
 * כך ששורה שנוספת מחר אינה נבלעת בשקט (כלל 11).
 */

// ---------------------------------------------------------------------------- skills
/**
 * ארבעה שמות בתסריט, חמישה כישורים במנוע, ושלוש הכרעות.
 *
 * `organization` הוא אותו כישור בשני השמות. השלושה האחרים הם המקצוע שהתסריט מתאר,
 * מתורגם ליכולת שהמנוע כבר סופר:
 *
 * · `documentation` → `knowledge` — לתעד זה לדעת מה קרה ולשמור את זה בצורה שאפשר לחזור
 *   אליה, וזה בדיוק מה ש-`knowledge` מודד כאן מאז שהוא נכתב (הוא גם הכניסה היחידה
 *   שמזינה גם אישיות וגם כישור).
 * · `enterprise` → `business` — יוזמה עסקית היא הכישור העסקי, ו-`OWNER` בנוי עליו.
 * · `mediation` → `communication` — לתווך בין שני צדדים זה מה ש-`communication` עושה
 *   בכל מסלול העיתונאי.
 *
 * אף אחת מהשלוש אינה מובנת מאליה, ולכן שלושתן כתובות. מי שחולק — משנה שורה.
 */
export const SKILL_OF: Readonly<Record<string, 'knowledge' | 'communication' | 'organization' | 'business' | 'creativity'>> = {
  organization: 'organization',
  documentation: 'knowledge',
  enterprise: 'business',
  mediation: 'communication',
}

// -------------------------------------------------------------------------- audiences
/**
 * שלושה קהלים מתוך ארבעה הם אותם קהלים בשם אחר; הרביעי אינו, ואומר את זה.
 *
 * · `terrace` → `gate5` — היציע. זה שער 5 וזה המקום היחיד שהוא.
 * · `basketball` → `ussishkin` — הכדורסל של המועדון הזה הוא האולם, וזה שם הקהל.
 * · `media` → `public` — מי שקורא. `JOURNALIST` נמדד מול `public` מאז שנכתב.
 *
 * ו-`international` **קיבל קהל משלו** (20.9.2026). הוא הושאר בחוץ ודווח, נשאל, ומאור
 * ענה: *"המטרה היא קהלים בינלאומיים שמזוהים עם ארגון ANTIFA, כמו סט פאולי."* זו תשובה
 * שמחייבת קהל שישי ולא קיפול — יציע בהמבורג שמכיר אותך אינו "הציבור" הישראלי בשום
 * מובן, **וגם אינו שער 7**: שער 7 הוא האוהדים שלנו בחוץ, וזה אוהדים של מישהו אחר
 * שעומדים אִתנו. `types.ts` נושא את ההנמקה המלאה ליד השורה.
 */
export const AUDIENCE_OF: Readonly<Record<string, ReputationAudience>> = {
  terrace: 'gate5',
  basketball: 'ussishkin',
  media: 'public',
  international: 'international',
}

// --------------------------------------------------------------------------- presence
/**
 * איך הוא היה שם. `inherit` הוא היחיד שאינו מצב אלא הוראה — *"מה שהפרק הזה כבר קבע"* —
 * ולכן הוא לא מתורגם למצב אלא מסומן ככזה ומדווח לקורא.
 */
export const PRESENCE_OF: Readonly<Record<string, 'inside' | 'late' | 'television'>> = {
  venue: 'inside',
  tv: 'television',
  later: 'late',
}
export const PRESENCE_INHERIT = 'inherit'

// -------------------------------------------------------------------------- the people
/**
 * עשרים וארבעה שמות בתסריט. חמישה־עשר מהם כבר ברישום הדמויות, שלושה הם **תפקידים**
 * ולא אנשים, והשאר חדשים.
 *
 * **`oli` הוא `uli`, ומאור הכריע (20.9.2026).** ברישום יש `uli`; בתסריט כתוב `oli`. זה
 * בדיוק המקרה שכלל 64 §5 אוסר לגשר עליו לבד — איות קרוב אינו ראיה. לכן הוא דווח,
 * נשאל, ונענה בשלוש אותיות: **"ULI"**. ההתאמה נכנסת עכשיו לא מפני שהיא נראית נכונה
 * אלא מפני שבעל הבית אמר אותה, וזה ההבדל שכל הכלל ההוא קיים בשבילו.
 */
export const CHARACTER_OF: Readonly<Record<string, string>> = {
  kobi: 'kobi',
  rachel: 'rachel',
  ofir: 'ofir',
  amit: 'amit',
  efi: 'efi',
  keren: 'keren',
  yosef: 'yosef',
  yaron: 'yaron',
  batya: 'batya',
  yonatan: 'yonatan',
  /**
   * נטע וגור — **שורות קיימות, לא אנשים חדשים** (21.9.2026).
   *
   * `N04` אומרת *"נטע וגור פועלים במוזיקה"*, וכתבתי להם שתי שורות חדשות במרשם. הן
   * היו כפילות: `neta-katamin` (נטע גופן) ו-`gur-katamin` (גור שפיגל) יושבים שם מאז
   * 20.9.2026 עם `aliases: ['נטע']` ו-`['גור']` בדיוק בשביל זה. `tests/life-cast.ts`
   * תפס את זה באותה ריצה — זו אותה תקלה שכלל 79 נכתב עליה (`ilan`), רק שהפעם היא
   * נתפסה על ידי שומר ולא על ידי שאלה.
   */
  neta: 'neta-katamin',
  gur: 'gur-katamin',
  melanie: 'melanie',
  dor: 'dor',
  shani: 'crowd-shani',
  // מאור, 20.9.2026, בתשובה לשאלה אם `oli` בתסריט הוא `uli` ברישום: "ULI".
  oli: 'uli',
  // שבע שורות שנוספו ל-`characters.ts` ב-20.9.2026, כל אחת נקראה מהסצנה שמציגה אותה
  metuki: 'metuki',
  roma: 'roma',
  lina: 'lina',
  nico: 'nico',
  tamar: 'tamar',
  alex: 'alex',
  // ענף הבעלות (היסטוריה חלופית), נוספו 20.9.2026
  michal: 'michal',
  adam: 'adam',
  // מאור, 20.9.2026, בתשובה אם `ilan` בתסריט הוא `neighbour` ברישום: "אילן השכן זה אילן כן."
  ilan: 'neighbour',
}

/**
 * תפקיד, לא אדם — נפתר בזמן ריצה מתוך מסלולי החיים ומתוך ההקשר של הסצנה.
 *
 * `partner` ו-`child` הם `PARTNERSHIP` ו-`PARENTHOOD` ב-`lib/life/tracks.ts`: מי הם
 * תלוי במה שהשחקן בחר, ושורה בטבלה שהייתה קובעת שם היא המשחק בוחר בשבילו בת זוג.
 * `target` הוא "מי שהסצנה הזאת מדברת איתו", ומשתנה בין הסתעפויות.
 */
export const ROLE_NOT_PERSON: readonly string[] = ['partner', 'child', 'target']

/**
 * שמות שהתסריט מביא ואין להם שורה ברישום. הם אינם שגיאה — הם עבודה: דמות היא שורה
 * לפני שהיא פנים (כלל 58), וכל אחד מהם צריך שורה ב-`characters.ts` עם גיל, עידנים
 * ובלי `portraitSet` עד שיש פלייט. הרשימה כאן כדי שהדוח יידע לספור אותם.
 */
export const PEOPLE_WITHOUT_A_ROW: readonly string[] = []

/**
 * ...ו-**התנגשות שם** היא הסוג האחר של חוסר, וגם היא נסגרה.
 *
 * `characters.ts` החזיק מאז 5.9.2026 את `neighbour` בשם **אילן השכן**, והתסריט כותב
 * `ilan` בחלון הכורסה. אותו אדם עשרים שנה אחר כך, או אדם אחר באותו שם — שתיהן היו
 * סבירות, ואף אחת מהן אינה ראיה (כלל 64 §5). זה `oli`/`uli` שוב, והתשובה הגיעה מאותו
 * מקום: **מאור, 20.9.2026 — "אילן השכן זה אילן כן."**
 *
 * הטבלה נשארת כאן ריקה בכוונה, ולא נמחקת: היא המקום שבו ההתנגשות הבאה תדווח. טבלה
 * שנמחקת אחרי שנפתרה היא כלי שלא יכול להיכשל (כלל 73).
 */
export const NAME_COLLISION: Readonly<Record<string, string>> = {}

// -------------------------------------------------------------------- the achievements
/**
 * שבעה מקטעי `הישג` בתסריט → **שישה** הישגים במנוע (21.9.2026).
 *
 * `keys_in_hand` מופיע פעמיים ב-`O04`, בשתי בחירות שונות, והוא אותו הישג — ולכן
 * הטבלה הזאת היא גם מה שמונע מהמנוע לספור שבעה. שאר החמישה הם אחד-לאחד.
 *
 * כל השישה נושאים `waitingHe` ב-`achievements.ts`, כי הפרקים שלהם (F04, J03, O03,
 * O04) עדיין לא נבנו. זו אינה סתירה: השורה קיימת כדי **שהסצנה תיכתב מולה**, והשורה
 * שאומרת מה חסר היא בדיוק מה שכלל 78 דורש.
 */
export const ACHIEVEMENT_OF: Readonly<Record<string, string>> = {
  forty_years_together: 'ACH_FORTY_YEARS',
  three_generations: 'ACH_THREE_GENERATIONS',
  reunion_in_europe: 'ACH_REUNION_EUROPE',
  asked_for_my_work: 'ACH_ASKED',
  not_at_any_price: 'ACH_NOT_AT_ANY_PRICE',
  keys_in_hand: 'ACH_KEYS',
}

// --------------------------------------------------------------------------- the costs
/**
 * `עלויות` הוא שלושה שדות ושלושתם קיימים במנוע כמות שהם: `minutes` הוא שעון המשחק,
 * `energy` הוא האנרגיה, ו-`cash` הוא **אגורות** — 5,000 עד 480,000, כלומר 50 ₪ עד
 * 4,800 ₪, שזה הסדר גודל של מבוגר שקונה נסיעה לאירופה ולא של ילד שקונה חולצה.
 * שלושתם עלויות, כלומר נרשמים בסימן הפוך.
 */
export const COST_FIELDS: readonly ['minutes', 'energy', 'cash'] = ['minutes', 'energy', 'cash']

// -------------------------------------------------------------------- what has no home
/**
 * מקטעים שהתסריט כותב ולמנוע אין להם מקום **עדיין**, כל אחד עם מה שהוא דורש.
 *
 * זו רשימת העבודה של החיבור, והיא כתובה כאן ולא בראש של מישהו. מקטע שאינו ברשימה
 * הזאת ואינו ממופה מפיל את הבדיקה, כך שהתסריט לא יכול לגדול בשקט מעבר למה שהמנוע יודע.
 */
export const NEEDS_A_HOME: Readonly<Record<string, string>> = {
  'פתיחת יחידת תוכן': 'פותח יחידת תוכן בשם — צריך רישום של יחידות ודגל פתיחה',
  'עניין פתוח': 'חוט פתוח שנשאר פתוח בין פרקים — אין לו מבנה במנוע',
  הבטחה: 'הבטחה עם `id`/`npc`/`due`/`kind` — יש `promise:` כדגל, אין מבנה עם מועד',
  'תקציב עסקי': 'חלוקת תקציב לחמישה סעיפים — ענף הבעלות, אין לו מודל',
  תיקון: 'תיקון פומבי — `public_correction` קיים כראיה, לא כמצב שממתין',
  'קופה משותפת': 'קופה משותפת עם אדם אחר — אין ארנק שני',
  'מיני־משחק': 'פותח מיני-משחק בשם — `team_match` אינו אחד מהקיימים',
  'יכולת מגרש': 'קריאה וטכניקה — שני מדדים שאינם כישורים ואינם אישיות',
  'הכנסה באגורות': 'הכנסה חוזרת — הארנק יודע סכום, לא קצב',
  עומס: 'עומס — קרוב ל-`wellbeing`, ולא זהה לאף ציר קיים',
  'מצב רגעי': 'מצב רגעי — נקודתי, אין לו שדה',
  'שיוך מפורש': 'שיוך מפורש של עמדה — הכרעה של מאור, לא של מיפוי',
  'הערת מחבר': 'הערה לקורא, לא אפקט — נשמרת ואינה מתורגמת',
  תיאום: 'תיאום בין שני אנשים (`pogi|ofir`) — היחסים כאן הם בין הדמויות ולא של השחקן איתן',
}
